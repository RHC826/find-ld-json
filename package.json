{
  "name": "find-ld-json",
  "version": "1.0.0",
  "description": "ウェブページ内に ld+json があれば取得する",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc index.ts",
    "minify": "tsc index.ts && npx terser index.js --output ./index.min.js --compress --mangle"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.29.2",
    "typescript": "^5.4.3"
  }
}
